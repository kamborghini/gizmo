# Only for the per-variant M5-style model, which is OFF by default
# (FORECAST_M5=1 turns it on). Install alongside requirements-service.txt:
#     pip install -r forecast/requirements-service.txt -r forecast/requirements-m5.txt
# Kept out of the default image because it is the larger part of it and serves
# a model this business does not use.
scikit-learn>=1.3
lightgbm>=4.3
catboost>=1.2
