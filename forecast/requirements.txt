# The forecasting package's own dependencies. Install into a separate
# environment; the Starlette app never imports this package.
numpy>=1.26
pandas>=2.1
openpyxl>=3.1
scipy>=1.11
scikit-learn>=1.3
lightgbm>=4.3          # macOS: brew install libomp
catboost>=1.2          # optional: drop it and pass --no-catboost
torch>=2.2             # optional: drop it and pass --no-nbeats
