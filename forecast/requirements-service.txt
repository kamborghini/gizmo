# The nightly service's image: the GBDTs without PyTorch, so the image stays
# a few hundred megabytes. Add torch and set FORECAST_NBEATS=1 to train N-BEATS.
numpy>=1.26
pandas>=2.1
openpyxl>=3.1
scipy>=1.11
scikit-learn>=1.3
lightgbm>=4.3
catboost>=1.2
# Holt-Winters and the simple exponential smoothing behind Theta, for the
# five plain models in simple.py. Small, pure-Python over numpy/scipy.
statsmodels>=0.14
# More voices for the sanity panel: AutoETS, AutoARIMA, AutoTheta, AutoCES.
# Optional at runtime - simple.py skips them when the import fails - but
# shipped, because a combination of many forecasts is worth more than any
# one of them and needs several to average.
statsforecast>=2.0
