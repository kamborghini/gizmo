# Environment variables — never commit these
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Icon library source — icons are inlined into static/index.html, no need to ship these
feather/

# Shopify CLI local state
.shopify/

# Third-party JS: package.json + package-lock.json reproduce it with npm install.
# Committing it shipped 30MB of unreviewed code into the repo and, via the
# Dockerfile COPY, into the production image.
node_modules/
